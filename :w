


#tool ssh
## keygen
ssh-keygen -t ed25519 -f <hostname>
ssh-agent -s
ssh-add $home/.ssh/<private_key>
# {
1) create the key
2) start ssh agent with -s flag so environment variables get exported to the shell -- SSH_AGENT_SOCK, SSH_AGENT_PID, etc.
3) add the key to the keyring
# }
